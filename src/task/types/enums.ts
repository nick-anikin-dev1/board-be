export enum  Priority {
  Low = 'Low',
  Medium = 'Medium',
  High = 'High',
}

export enum Status {
  Backlog = 'Backlog',
  New = 'New',
  ToDo = 'ToDo',
  InProgress = 'In progress',
  Done = 'Done',
}

export enum Type {
  Epic = 'Epic',
  Story = 'Story',
  Task = 'Task',
}